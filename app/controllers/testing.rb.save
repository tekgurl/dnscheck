require 'hanami/controller'

module Testing
  class Results
    include ::Hanami::Action
    def call(params)
#      self.body = "Post #{params[:domain]}"
        def results
          dns.each_resource(domain, Resolv::DNS::Resource::IN::A) do |a|
            puts a.address
          
      self.body = results
    end
  end
end
